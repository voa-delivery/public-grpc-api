spectype: gendocu
servers:
  - selector: "*"
    envs:
      - name: "Production"
        urls:
          - grpcs://grpc.voa.delivery:443
securitySchemes:
  - name: token-private
    title: Token Auth
    description:
      text: APP_KEY identifica a plataforma parceira. TOKEN identifica o cliente da plataforma parceira e deve ser gerado pelo painel da voa.delivery.
    callAuthentication:
      token:
        exampleValue: "..."
        grpc:
          meta: "TOKEN"
        grpcweb:
          header: "APP_KEY"